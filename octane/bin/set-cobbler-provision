#!/bin/python

import sys
import yaml

filename = sys.argv[1]

with open(filename) as f:
    settings = yaml.safe_load(f)
    settings["editable"]["provision"]["method"]["value"] = "cobbler"
    yaml.dump(settings, stream=sys.stdout, default_flow_style=False)
